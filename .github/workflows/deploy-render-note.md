# Render Deployment Note

This repository includes `render.yaml`.

Use Render Blueprint deployment from the repository to create the long-running Node Web Service.
After Render gives an HTTPS URL, run the `Verify Toybox FPS Arena` workflow manually with that URL in `public_url`.
